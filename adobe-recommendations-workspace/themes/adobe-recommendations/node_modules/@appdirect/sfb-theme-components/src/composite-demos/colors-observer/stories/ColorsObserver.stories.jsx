import React from 'react';
import { ColorsObserver } from '../ColorsObserver';
import { getArgs } from '../../../tools/controlsHelper';

const customArgTypes = {
    primaryColor: {
        control: 'color'
    },
    secondaryColor: {
        control: 'color'
    },
    emphasisColor: {
        control: 'color'
    },
    altEmphasisColor: {
        control: 'color'
    }
};

export default {
    title: 'Composite Demos/Colors Observer',
    component: ColorsObserver,
    argTypes: customArgTypes,
    parameters: {
        controls: {
            include: Object.keys(customArgTypes)
        }
    }
};

export const Default = {
    render: args => <ColorsObserver settings={args} />,
    args: getArgs(ColorsObserver.schema().form)
};
