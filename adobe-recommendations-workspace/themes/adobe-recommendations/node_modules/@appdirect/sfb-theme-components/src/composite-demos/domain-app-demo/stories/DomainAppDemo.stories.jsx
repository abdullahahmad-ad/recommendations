import React from 'react';

import { TabsMenuComponent } from '../../../components/product-tabs-menu/TabsMenu';
import { DomainAppDemo } from '../DomainAppDemo';
import { navigationMock } from '../../../../mocks/product/navigation';
import { DomainAppComponent } from '../../../components/domain-app/DomainApp';
import { domainAppMockCheckoutV2 } from '../../../../mocks/product/domainApp';
import { fetchSuggestionsMock } from '../../../../mocks/product/domainSuggestionFetch';

export default {
    title: 'Composite Demos/Domain App Demo',
    component: DomainAppDemo,
    parameters: {
        layout: 'fullscreen',
        container: 'fluid'
    }
};

export const Default = () => {
    const tabs = <TabsMenuComponent data={navigationMock} />;
    const domainApp = (
        <DomainAppComponent
            data={domainAppMockCheckoutV2}
            fetchSuggestions={fetchSuggestionsMock}
        />
    );
    return <DomainAppDemo tabs={tabs} domainApp={domainApp} />;
};
