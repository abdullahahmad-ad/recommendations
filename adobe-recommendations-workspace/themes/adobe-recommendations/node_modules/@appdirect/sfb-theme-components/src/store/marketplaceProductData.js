import create from 'zustand';

export const marketplaceProductDataStore = create(set => ({
    marketplaceProductData: {},
    setMarketplaceProductData: marketplaceProductData => set(() => ({ marketplaceProductData }))
}));
