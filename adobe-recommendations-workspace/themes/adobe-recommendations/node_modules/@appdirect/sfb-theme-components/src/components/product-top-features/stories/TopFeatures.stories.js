import React from 'react';
import ProductTopFeatures from '../TopFeatures';
import { featuresMock } from '../../../../mocks/product/top-features';
import { getArgTypes, getArgs } from '../../../tools/controlsHelper';

export default {
    title: 'Components/Product Top Features',
    component: ProductTopFeatures,
    argTypes: getArgTypes(ProductTopFeatures.schema().form),
    render: args => <ProductTopFeatures data={featuresMock} settings={args} />,
    parameters: {
        controls: {
            include: Object.keys(getArgTypes(ProductTopFeatures.schema().form))
        }
    }
};

export const Default = {
    args: getArgs(ProductTopFeatures.schema().form)
};

export const WithSettings = {
    args: getArgs(ProductTopFeatures.schema().form)
};
