import React from 'react';
import ProductReviews from '../../../atoms/product-reviews/ProductReviews';
import { getArgTypes, getArgs } from '../../../tools/controlsHelper';
import { reviewsMock } from '../../../../mocks/product/reviews';

const reviewsData = { ...reviewsMock };

export default {
    title: 'Components/Product Reviews',
    component: ProductReviews,
    argTypes: getArgTypes(ProductReviews.schema().form),
    render: args => (
        <ProductReviews data={reviewsData} settings={args} />
    ),
    parameters: {
        controls: {
            include: Object.keys(ProductReviews.schema().form)
        }
    }
};

export const Default = {
    args: getArgs(ProductReviews.schema().form)
};
