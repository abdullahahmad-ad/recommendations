import React from 'react';
import { FaqComponent } from '../Faq';
import { featuresMock } from '../../../../mocks/product/faq';
import { getArgTypes, getArgs } from '../../../tools/controlsHelper';

export default {
    title: 'Components/Faq',
    component: FaqComponent,
    argTypes: getArgTypes(FaqComponent.schema().form),
    parameters: {
        controls: {
            include: Object.keys(FaqComponent.schema().form)
        }
    }
};

export const Default = {
    render: args => <FaqComponent data={featuresMock} settings={args} />,
    args: getArgs(FaqComponent.schema().form)
};
