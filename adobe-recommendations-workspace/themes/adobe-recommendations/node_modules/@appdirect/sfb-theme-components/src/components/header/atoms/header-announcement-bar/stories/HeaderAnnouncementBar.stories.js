import React from 'react';
import { getArgTypes, getArgs } from '../../../../../tools/controlsHelper';
import HeaderAnnouncementBar from '../HeaderAnnouncementBar';

export default {
    title: 'Global Components/Header/Atoms/Header Announcement Bar',
    component: HeaderAnnouncementBar,
    argTypes: getArgTypes(HeaderAnnouncementBar.schema().form),
    parameters: {
        layout: 'fullscreen'
    }
};

export const Default = {
    render: args => <HeaderAnnouncementBar settings={args} />,
    args: {
        ...getArgs(HeaderAnnouncementBar.schema().form),
        announcementBarMessage: 'This is a test announcement bar message',
        announcementBarLink: 'https://www.appdirect.com',
        announcementBarHeight: 60
    }
};
