import React from 'react';
import { footerDataMock, footerMenusMock } from '../../../../mocks/components/footer';
import Footer from '../Footer';
import { getArgTypes, getArgs } from '../../../tools/controlsHelper';

export default {
    title: 'Global Components/Footer',
    component: Footer,
    argTypes: getArgTypes(Footer.schema().form),
    parameters: {
        controls: {
            include: Object.keys(getArgTypes(Footer.schema().form))
        },
        layout: 'fullscreen'
    }
};

export const Default = {
    render: args => <Footer data={footerDataMock} settings={args} menus={footerMenusMock} />,
    args: getArgs(Footer.schema().form)
};
