import React from 'react';
import { headerDataMock, headerDataMockLoggedIn, headerSettingsFullMocks } from '../../../../mocks/components/header';
import Header from '../Header';

export default {
    title: 'Global Components/Header',
    component: Header,

    parameters: {
        layout: 'fullscreen'
    }
};

export const Default = {
    render: () => <Header data={headerDataMock} settings={headerSettingsFullMocks} />,
    name: 'Default'
};

export const WithUserLoggedIn = {
    render: () => <Header data={headerDataMockLoggedIn} settings={headerSettingsFullMocks} />,
    name: 'With user logged in'
};
