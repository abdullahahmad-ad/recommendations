import React from 'react';
import { getArgTypes, getArgs } from '../../../tools/controlsHelper';
import { ProductSimilarAppsComponent as ProductSimilarApps } from '../ProductSimilarApps';
import { productSimilarAppsMock } from '../../../../mocks/components/productSimilarApps';

export default {
    title: 'Components/Product Similar Apps',
    component: ProductSimilarApps,
    argTypes: getArgTypes(ProductSimilarApps.schema().form),
    render: args => (
        <ProductSimilarApps data={productSimilarAppsMock} settings={args} />
    ),
    parameters: {
        controls: {
            include: Object.keys(ProductSimilarApps.schema().form)
        }
    }
};

export const Default = {
    args: getArgs(ProductSimilarApps.schema().form)
};

export const WithSettings = {
    args: getArgs(ProductSimilarApps.schema().form)
};
