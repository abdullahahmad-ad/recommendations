import React from 'react';
import SecondaryNav from '../SecondaryNav';
import { secondaryNavMock } from '../../../../mocks/menu/secondary-nav';
import { categoriesMock } from '../../../../mocks/menu/categories';

const data = {
    primaryNav: categoriesMock,
    secondaryNav: secondaryNavMock
};

const defaultArgs = {
    backgroundColor: '#BBFAF4'
};

export default {
    title: 'Components/Secondary Navigation',
    component: SecondaryNav,
    argTypes: {
        backgroundColor: {
            control: 'color',
            description: 'Background color of the secondary navigation'
        }
    },
    render: args => <SecondaryNav data={data} settings={args} />
};

export const Default = {
    args: defaultArgs
};

export const WithSettings = {
    args: defaultArgs
};
