import React from 'react';
import ProductListing from '../ProductListing';
import { listingProductsMock } from '../../../../mocks/listingProducts';
import { getArgTypes, getArgs } from '../../../tools/controlsHelper';

export default {
    title: 'Components/Product Listing',
    component: ProductListing,
    argTypes: getArgTypes(ProductListing.schema().form),
    render: ({ showAsGrid, defaultView, ...args }) => (
        <ProductListing
            data={{
                ...listingProductsMock,
                showAsGrid,
                defaultView
            }}
            settings={args}
        />
    )
};

export const AsList = {
    args: {
        ...getArgs(ProductListing.schema().form),
        defaultView: 'list-view',
        showAsGrid: false
    }
};

export const AsGrid = {
    args: {
        ...getArgs(ProductListing.schema().form),
        defaultView: 'grid-view',
        showAsGrid: true
    }
};
