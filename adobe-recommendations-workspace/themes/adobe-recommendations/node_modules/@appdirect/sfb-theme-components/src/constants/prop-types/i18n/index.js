import PropTypes from 'prop-types';
import { i18nCartShape } from './cart';
import { i18nHeaderShape } from './header';

export const i18nShape = {
    BillingAndDistribution: PropTypes.string,
    Home: PropTypes.string,
    Insights: PropTypes.string,
    'List.all.apps': PropTypes.string,
    Manage: PropTypes.string,
    'My.Apps': PropTypes.string,
    'My.Company': PropTypes.string,
    'My.Profile': PropTypes.string,
    'My.Account': PropTypes.string,
    Teams: PropTypes.string,
    appdirectadmin: PropTypes.string,
    channel: PropTypes.string,
    corporate: PropTypes.string,
    'find.applications': PropTypes.string,
    'find.products': PropTypes.string,
    logout: PropTypes.string,
    'my.settings': PropTypes.string,
    viewall: PropTypes.string,
    showAll: PropTypes.string,
    seeMore: PropTypes.string,
    defaultTitlePlaceholder: PropTypes.string,
    defaultDescriptionPlaceholder: PropTypes.string,
    defaultPricePlaceholder: PropTypes.string,
    reseller: PropTypes.string,
    'sign.in': PropTypes.string,
    'sign.up': PropTypes.string,
    learnMore: PropTypes.string,
    Contact: PropTypes.string,
    'Call.us.toll.free': PropTypes.string,
    'Powered.by.AppDirect': PropTypes.string,
    'help.center': PropTypes.string,
    pricing: PropTypes.string,
    free: PropTypes.string,
    profile: PropTypes.string,
    account: PropTypes.string,
    company: PropTypes.string,
    settings: PropTypes.string,
    allProducts: PropTypes.string,
    backToMainMenu: PropTypes.string,
    backToResults: PropTypes.string,
    skipToMain: PropTypes.string,
    closeAlert: PropTypes.string,
    previous: PropTypes.string,
    next: PropTypes.string,
    pause: PropTypes.string,
    play: PropTypes.string,
    playVideo: PropTypes.string,
    carouselPlay: PropTypes.string,
    carouselPause: PropTypes.string,
    genericErrorMessage: PropTypes.string,
    cart: PropTypes.shape(i18nCartShape),
    header: PropTypes.shape(i18nHeaderShape)
};
