import PropTypes from 'prop-types';
import { i18nPropTypes } from './i18n';

export const crossSellManagementPropTypes = {
    selectedEdition: PropTypes.string,
    selectedAddons: PropTypes.arrayOf(PropTypes.string),
    previousCTA: PropTypes.shape({
        actionType: PropTypes.string
    }),
    i18n: PropTypes.shape(i18nPropTypes)
};
