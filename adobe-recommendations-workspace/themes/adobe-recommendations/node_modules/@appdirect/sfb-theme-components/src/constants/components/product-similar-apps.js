import { recommendationsMock } from '../../../mocks/application/recommendations';
import { productSliderDefaultSettings } from './schemas/product-slider';

export const productSimilarAppsDefaultData = {
    recommendations: recommendationsMock
};

export const productSimilarAppsDefaultSettings = {
    ...productSliderDefaultSettings,
    sliderTitle: undefined,
    sortBy: null
};
