import { LEFT, RIGHT } from '../alignment';
import { COL_ONE, COL_TWO } from '../columns';

export const INFO_POSITION_OPTIONS = [RIGHT, LEFT];
export const COLUMN_COUNT_OPTIONS = [COL_ONE, COL_TWO];
