import PropTypes from 'prop-types';

export const orderableComponentsShape = PropTypes.shape({
    id: PropTypes.string,
    name: PropTypes.string,
    data: PropTypes.object,
    settings: PropTypes.object,
    menus: PropTypes.object
});
