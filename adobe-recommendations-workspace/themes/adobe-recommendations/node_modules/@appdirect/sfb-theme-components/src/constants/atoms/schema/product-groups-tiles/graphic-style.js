import { cardStyleForm } from './card-style';

import GraphicStyle from '../../../../atoms/product-groups-tiles/graphic-style/GraphicStyle';

// remove unneeded props from CardStyle
const {
    style,
    displayUpperTitle,
    upperTitleAlignment,
    cardBackgroundColor,
    cardCornerRadius,
    upperTitle,
    upperTitleColor,
    imageMask,
    ...graphicStyleForm
} = cardStyleForm;

GraphicStyle.schema = () => ({
    name: 'ProductGroupGraphicStyle',
    title: 'uieditor.sfbComponent.productGroupGraphicStyle.title',
    form: graphicStyleForm
});

export default GraphicStyle;
