import { useEffect } from 'react';

export const useBodyPageAttribute = value => {
    useEffect(() => {
        document.body.dataset.page = value;

        return () => {
            delete document.body.dataset.page;
        };
    }, [value]);
};
