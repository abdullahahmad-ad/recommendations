import React from 'react';
import { reviewsMock as ratingMock,
    reviewsFractionalMock as ratingFractionalMock } from '../../../../mocks/product/related-content/details/reviews';
import Rating from '../Rating';

export default {
    title: 'Atoms/Rating',
    component: Rating
};

export const Default = {
    render: () => <Rating {...ratingMock} />,
    name: 'Default'
};

export const Fractional = {
    render: () => <Rating {...ratingFractionalMock} />,
    name: 'Fractional'
};
