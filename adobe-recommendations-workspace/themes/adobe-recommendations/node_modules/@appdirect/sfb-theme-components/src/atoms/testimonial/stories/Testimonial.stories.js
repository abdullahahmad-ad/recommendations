import React from 'react';
import Testimonial from '../Testimonial';
import { testimonialMock } from '../../../../mocks/product/testimonials';

const testimonialList = testimonialMock.results;

export default {
    title: 'Atoms/Testimonial',
    component: Testimonial
};

export const Default = {
    render: () => <Testimonial {...testimonialList[0]} />,
    name: 'Default'
};

export const NoImage = {
    render: () => <Testimonial {...testimonialList[1]} />,
    name: 'No image'
};
