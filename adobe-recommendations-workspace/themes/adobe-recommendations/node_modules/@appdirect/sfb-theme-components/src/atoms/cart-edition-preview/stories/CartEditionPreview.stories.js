import React from 'react';
import CartEditionPreview from '../CartEditionPreview';
import { cartMock, editionProps, editionPropsWithoutIconUrl } from '../../../../mocks/cart';

export default {
    title: 'Atoms/Cart Edition Preview',
    component: CartEditionPreview
};

export const Default = {
    render: () => <CartEditionPreview {...editionProps} i18n={cartMock.i18n} />,
    name: 'Default'
};

export const PlaceholderImage = {
    render: () => <CartEditionPreview {...editionPropsWithoutIconUrl} i18n={cartMock.i18n} />,
    name: 'Placeholder image'
};
