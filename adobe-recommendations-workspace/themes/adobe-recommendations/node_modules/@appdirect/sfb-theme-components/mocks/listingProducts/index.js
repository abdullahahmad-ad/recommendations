import { i18nMock } from './i18n';
import { listingItemsMock } from './items';
import { paginationMock } from './pagination';
import { sortingOptionsMock } from './sorting';

export const listingProductsMock = {
    items: listingItemsMock,
    pagination: paginationMock,
    query: {},
    homeUrl: '/',
    compare: {
        baseUrl: '/en-US/compare'
    },
    sorting: sortingOptionsMock,
    i18n: i18nMock
};

export const singleProductListingDataMock = listingProductsMock.items[0];

export const singleProductMockListingDataMockWithoutIcon = {
    ...listingProductsMock.items[0],
    iconUrl: null,
    iconSrcset: {}
};
