import { DYNAMICALLY_CURATED_CONTENT_TYPE } from '../../src/constants/components/slider';
import { popularAppsMock } from '../productLists/popularApps';

export const sliderMock = {
    ...popularAppsMock,
    contentType: DYNAMICALLY_CURATED_CONTENT_TYPE
};

export const sliderWithShowMoreMock = {
    ...sliderMock,
    buttonLabel: 'See More'
};
