import create from 'zustand';

export const profilePageDataStore = create(set => ({
    isFeaturesLinkEnabled: true,
    setIsFeaturesLinkEnabled: isFeaturesLinkEnabled => set(() => ({ isFeaturesLinkEnabled }))
}));
