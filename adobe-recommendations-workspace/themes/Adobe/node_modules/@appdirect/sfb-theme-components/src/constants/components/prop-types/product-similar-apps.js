import PropTypes from 'prop-types';
import { productSliderSettingsShape } from './product-slider';
import { tileShape } from './tile';

export const productSimilarAppsSettingsShape = {
    ...productSliderSettingsShape,
    sortBy: PropTypes.string
};

export const productSimilarAppsDataShape = {
    recommendations: PropTypes.shape({
        items: PropTypes.arrayOf(tileShape),
        viewAllLink: PropTypes.string,
        i18n: PropTypes.shape({
            title: PropTypes.string,
            titleProduct: PropTypes.string,
            viewAll: PropTypes.string
        })
    })
};
