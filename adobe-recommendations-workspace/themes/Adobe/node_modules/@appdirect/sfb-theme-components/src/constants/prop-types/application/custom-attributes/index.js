import PropTypes from 'prop-types';

export const customAttributesPropTypes = {
    type: PropTypes.string,
    attributeKey: PropTypes.string,
    label: PropTypes.string,
    value: PropTypes.string
};
