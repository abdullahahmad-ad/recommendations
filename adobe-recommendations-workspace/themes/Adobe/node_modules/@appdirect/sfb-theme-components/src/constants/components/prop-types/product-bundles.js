import PropTypes from 'prop-types';
import { i18nPropTypes } from '../../prop-types/bundle/i18n';
import { itemPropTypes } from '../../prop-types/bundle/items';
import { pricingPropTypes } from '../../prop-types/bundle/pricing';
import { summaryPropTypes } from '../../prop-types/bundle/summary';

export const productBundlesPropTypes = {
    data: PropTypes.shape({
        summary: PropTypes.shape(summaryPropTypes),
        items: PropTypes.arrayOf(PropTypes.shape(itemPropTypes)),
        pricing: PropTypes.shape(pricingPropTypes),
        i18n: PropTypes.shape(i18nPropTypes)
    })
};
