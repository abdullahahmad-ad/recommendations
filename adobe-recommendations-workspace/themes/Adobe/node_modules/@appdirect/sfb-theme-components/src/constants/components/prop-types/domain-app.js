import PropTypes from 'prop-types';
import { callToActionsShape as applicationCallToActionsShape } from '../../prop-types/application/call-to-actions';
import { configShape as applicationConfigShape } from '../../prop-types/application/config';
import { domainsShape as applicationDomainsShape } from '../../prop-types/application/domains';
import { configShape } from '../../prop-types/config';

export const domainAppDataShape = {
    config: PropTypes.shape(configShape),
    application: PropTypes.shape({
        config: PropTypes.shape(applicationConfigShape),
        callToActions: PropTypes.shape(applicationCallToActionsShape),
        domains: PropTypes.shape(applicationDomainsShape)
    })
};
