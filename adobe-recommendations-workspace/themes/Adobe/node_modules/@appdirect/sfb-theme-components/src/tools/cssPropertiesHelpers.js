export const getCssPropertyURL = cssPropertyName => {
    const cssPropertyVariable = getComputedStyle(document.documentElement).getPropertyValue(`--${cssPropertyName}`);
    const match = cssPropertyVariable.match(/(?:url\('|")(.*)(?='|"\))/);
    return match ? match[1].trim() : '';
};
