export const getImageSrc = (imgSrc, imgSrcset) => {
    if (!imgSrc && !imgSrcset) {
        return null;
    }

    const srcset = imgSrcset && (imgSrcset['2x'] || imgSrcset['1x'] || imgSrcset.default);

    return srcset || imgSrc;
};
