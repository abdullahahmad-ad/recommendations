// check if element width is more than viewport width divided by given number
export const isMoreThanAllowedWidth = (elem, divider = 1) => {
    const viewportWidth = Math.max(document.documentElement.clientWidth || 0, window.innerWidth || 0);
    return elem.clientWidth > (viewportWidth / divider);
};
