import React from 'react';
import ProductQuestionForm from '../ProductQuestionForm';
import { questionsMock } from '../../../../mocks/product/questions';

export default {
    title: 'Components/Product Question Form',
    component: ProductQuestionForm
};

export const Default = {
    render: () => <ProductQuestionForm data={questionsMock} />,
    name: 'Default'
};
