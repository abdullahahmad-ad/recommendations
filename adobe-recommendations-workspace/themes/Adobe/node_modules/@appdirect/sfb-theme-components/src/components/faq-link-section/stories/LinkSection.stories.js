import React from 'react';
import LinkSection from '../LinkSection';
import { getArgTypes, getArgs } from '../../../tools/controlsHelper';
import { linkSectionDataMock } from '../../../../mocks/menu/link-section';

export default {
    title: 'Components/Link Section',
    component: LinkSection,
    argTypes: getArgTypes(LinkSection.schema().form),
    parameters: {
        controls: {
            include: Object.keys(LinkSection.schema().form)
        }
    }
};

export const Default = {
    render: args => <LinkSection data={linkSectionDataMock} settings={args} />,
    args: getArgs(LinkSection.schema().form)
};
