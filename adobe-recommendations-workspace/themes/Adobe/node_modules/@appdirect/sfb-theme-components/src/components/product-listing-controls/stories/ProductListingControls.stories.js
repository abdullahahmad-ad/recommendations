import React from 'react';
import ProductListingControls from '../ProductListingControls';
import { listingProductsMock } from '../../../../mocks/listingProducts';

export default {
    title: 'Components/Product Listing Controls',
    component: ProductListingControls
};

export const Default = {
    render: () => (
        <ProductListingControls data={listingProductsMock} />
    )
};
