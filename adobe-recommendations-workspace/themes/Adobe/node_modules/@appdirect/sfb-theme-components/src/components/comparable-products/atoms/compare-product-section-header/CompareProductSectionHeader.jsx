import React from 'react';
import PropTypes from 'prop-types';
import { createNamespace } from '../../../../tools/namingTools';
// IMPORTANT: (s)css should be imported after other atoms/components
import './styles/CompareProductSectionHeader.scss';

const n = createNamespace('CompareProductSectionHeader');

export const CompareProductSectionHeader = ({ label }) => (
    <div {...n('wrapper').props}>
        <span {...n('title').props}>{label}</span>
    </div>
);

CompareProductSectionHeader.propTypes = {
    label: PropTypes.string
};

CompareProductSectionHeader.defaultProps = {
    label: null
};
