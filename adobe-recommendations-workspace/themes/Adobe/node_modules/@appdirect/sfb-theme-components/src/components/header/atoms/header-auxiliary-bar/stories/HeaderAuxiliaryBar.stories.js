import React from 'react';
import { getArgTypes, getArgs } from '../../../../../tools/controlsHelper';
import HeaderAuxiliaryBar from '../HeaderAuxiliaryBar';
import { headerAuxiliaryBarDataMock } from '../../../../../../mocks/components/header';

export default {
    title: 'Global Components/Header/Atoms/Header Auxiliary Bar',
    component: HeaderAuxiliaryBar,
    argTypes: getArgTypes(HeaderAuxiliaryBar.schema().form),
    parameters: {
        layout: 'fullscreen'
    }
};

export const Default = {
    render: args => (
        <HeaderAuxiliaryBar settings={args} data={headerAuxiliaryBarDataMock} />
    ),
    args: {
        ...getArgs(HeaderAuxiliaryBar.schema().form),
        auxiliaryBarMessage: 'This is a test auxiliary bar message',
        auxiliaryBarLink: 'https://www.appdirect.com',
        auxiliaryBarHeight: 60
    }
};
