import React from 'react';
import { navigationMock as navigationData } from '../../../../mocks/product/navigation';
import TabsMenu from '../TabsMenu';
import { getArgTypes, getArgs } from '../../../tools/controlsHelper';

export default {
    title: 'Components/Tabs Menu',
    component: TabsMenu,
    argTypes: getArgTypes(TabsMenu.schema().form),
    render: args => <TabsMenu data={navigationData} settings={args} />,
    parameters: {
        controls: {
            include: Object.keys(getArgTypes(TabsMenu.schema().form))
        }
    }
};

export const Default = {
    args: getArgs(TabsMenu.schema().form)
};

export const WithSettings = {
    args: getArgs(TabsMenu.schema().form)
};
