import React from 'react';
import { ComparableProducts } from '../ComparableProducts';
import { comparableProductsMock } from '../../../../mocks/comparableProducts';
import { getArgTypes, getArgs } from '../../../tools/controlsHelper';

export default {
    title: 'Components/Comparable Products',
    component: ComparableProducts,
    argTypes: getArgTypes(ComparableProducts.schema().form),
    parameters: {
        controls: {
            include: Object.keys(ComparableProducts.schema().form)
        }
    }
};

export const Default = {
    render: args => (
        <ComparableProducts data={comparableProductsMock} settings={args} />
    ),
    args: getArgs(ComparableProducts.schema().form)
};
