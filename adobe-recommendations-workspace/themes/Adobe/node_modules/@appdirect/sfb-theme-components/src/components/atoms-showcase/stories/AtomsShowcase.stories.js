import React from 'react';
import AtomsShowcase from '../AtomsShowcase';
import { getArgTypes, getArgs } from '../../../tools/controlsHelper';

export default {
    title: 'Components/AtomsShowcase',
    component: AtomsShowcase,
    argTypes: {
        ...getArgTypes(AtomsShowcase.schema().form),
        ctaButtonLabel: {
            if: { arg: 'ctaButton', eq: true }
        }
    }
};

export const Default = {
    render: args => <AtomsShowcase settings={args} />,
    args: getArgs(AtomsShowcase.schema().form)
};
