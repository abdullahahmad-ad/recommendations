import React from 'react';
import ProductGroups from '../ProductGroups';
import { getArgTypes, getArgs } from '../../../tools/controlsHelper';
import { productGroupsTilesMocks } from '../../../../mocks/product/productGroupsTiles';

export default {
    title: 'Components/Product Groups',
    component: ProductGroups,
    argTypes: getArgTypes(ProductGroups.schema().form),
    render: args => <ProductGroups settings={{ ...args, productGroupsList: productGroupsTilesMocks }} />
};

export const Default = {
    args: getArgs(ProductGroups.schema().form)
};
