import React from 'react';
import { resourcesMock } from '../../../../mocks/application/resources';
import Resources from '../Resources';
import { getArgTypes, getArgs } from '../../../tools/controlsHelper';

const productResourcesData = {
    public: resourcesMock.public,
    reseller: resourcesMock.reseller,
    i18n: resourcesMock.i18n
};

export default {
    title: 'Components/Product Resources',
    component: Resources,
    argTypes: getArgTypes(Resources.schema().form),
    render: args => (
        <Resources
            data={productResourcesData}
            settings={args}
        />
    )
};

export const Default = {
    args: getArgs(Resources.schema().form)
};

export const WithSettings = {
    args: getArgs(Resources.schema().form)
};
