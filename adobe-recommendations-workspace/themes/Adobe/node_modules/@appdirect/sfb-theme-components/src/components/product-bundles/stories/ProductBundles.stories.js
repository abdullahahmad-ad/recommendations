import React from 'react';
import ProductBundles from '../ProductBundles';
import { bundleMock as productBundleMock } from '../../../../mocks/bundle';

export default {
    title: 'Components/Product Bundles',
    component: ProductBundles
};

export const Default = {
    render: () => (
        <ProductBundles
            data={{
                ...productBundleMock,
                items: productBundleMock.items.slice(0, 3)
            }}
        />
    ),

    name: 'Default'
};

export const CarouselBundles = {
    render: () => <ProductBundles data={productBundleMock} />,
    name: 'Carousel bundles'
};
