import React from 'react';
import ProductFeaturedCustomers from '../FeaturedCustomers';
import { customersMock } from '../../../../mocks/product/featured-customers';
import { getArgTypes, getArgs } from '../../../tools/controlsHelper';

const data = {
    overview: {
        ...customersMock
    }
};

export default {
    title: 'Components/Product Featured Customers',
    component: ProductFeaturedCustomers,
    argTypes: getArgTypes(ProductFeaturedCustomers.schema().form),
    render: args => <ProductFeaturedCustomers data={data} settings={args} />
};

export const WithTitleAndImages = {
    args: getArgs(ProductFeaturedCustomers.schema().form)
};

export const WithSettings = {
    args: getArgs(ProductFeaturedCustomers.schema().form)
};
