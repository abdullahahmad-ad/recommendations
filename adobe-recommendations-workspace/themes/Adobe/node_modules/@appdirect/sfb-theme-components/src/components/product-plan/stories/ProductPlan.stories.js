import React from 'react';
import ProductPlan from '../ProductPlan';
import { productPlanMock, productOnlyPlansMock, productOnlyNotes } from '../../../constants/components/product-plan';

export default {
    title: 'Components/Product Plan',
    component: ProductPlan
};

export const Default = {
    render: () => <ProductPlan data={productPlanMock} />,
    name: 'Default'
};

export const OnlyPlans = {
    render: () => <ProductPlan data={productOnlyPlansMock} />,
    name: 'Only plans'
};

export const OnlyNotes = {
    render: () => <ProductPlan data={productOnlyNotes} />,
    name: 'Only notes'
};
