import React from 'react';
import BundleCard from '../BundleCard';
import { getBundlePricing } from '../../../components/product-bundles/ProductBundles';
import { productBundleMock } from '../../../../mocks/product/productBundles';

const bundle = productBundleMock.bundle.items[0];

const bundleDataMock = {
    ...bundle,
    pricing: getBundlePricing(bundle.name, bundle.edition.name, productBundleMock.bundle.pricing.editions)
};

export default {
    title: 'Atoms/Bundle Card',
    component: BundleCard
};

export const Default = {
    render: () => <BundleCard {...bundle} />,
    name: 'Default'
};

export const WithPricing = {
    render: () => <BundleCard {...bundleDataMock} />,
    name: 'With pricing'
};
