import React from 'react';
import SelectedEdition from '../SelectedEdition';
import { selectedEditionsMock } from '../../../../mocks/product/configuratorSelectedEdition';

const selectedEditionData = selectedEditionsMock.configurator.selectedEdition;

export default {
    title: 'Atoms/Selected Edition',
    component: SelectedEdition
};

export const Default = {
    render: () => <SelectedEdition selectedEdition={selectedEditionData} />,
    name: 'Default'
};
