import React from 'react';
import PreviewModal from '../PreviewModal';

export default {
    title: 'Atoms/PreviewModal',
    component: PreviewModal
};

export const PreviewModalWithHeaderContent = {
    render: () => (
        <PreviewModal show title="Title" onClose={() => null}>
            Content
        </PreviewModal>
    ),

    name: 'Preview Modal with Header & Content'
};
