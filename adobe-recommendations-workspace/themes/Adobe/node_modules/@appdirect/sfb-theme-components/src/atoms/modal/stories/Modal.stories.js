import React from 'react';
import Modal from '../Modal';

export default {
    title: 'Atoms/Modal',
    component: Modal
};

export const Default = {
    render: () => (
        <Modal
            currentKey="story"
            currentItem={(
                <div
                    style={{
                        padding: '10px 20px'
                    }}
                >
                    Modal content
                </div>
            )}
        />
    ),

    name: 'Default',
    inline: false
};
