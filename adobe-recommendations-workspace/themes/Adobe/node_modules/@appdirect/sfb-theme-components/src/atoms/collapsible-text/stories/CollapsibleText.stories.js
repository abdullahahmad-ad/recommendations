import React from 'react';
import CollapsibleText from '../CollapsibleText';
import { featuresMock } from '../../../../mocks/product/faq';

const { items } = featuresMock.faqGroups;

export default {
    title: 'Atoms/Collapsible Text',
    component: CollapsibleText
};

export const Default = {
    render: () => <CollapsibleText faqData={items[0].faqs[0]} />,
    name: 'Default'
};

export const Expandable = {
    render: () => <CollapsibleText isActive isExpandable faqData={items[0].faqs[0]} />,
    name: 'Expandable'
};
