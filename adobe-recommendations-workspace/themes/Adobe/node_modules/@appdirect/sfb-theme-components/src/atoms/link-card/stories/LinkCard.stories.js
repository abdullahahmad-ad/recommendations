import React from 'react';
import { LinkCard } from '../LinkCard';
import { linkCardMock } from '../../../../mocks/product/link-card';

export default {
    title: 'Atoms/LinkCard',
    component: LinkCard
};

export const Default = {
    render: () => <LinkCard linkCard={linkCardMock} iconType="user" />,
    name: 'Default'
};
