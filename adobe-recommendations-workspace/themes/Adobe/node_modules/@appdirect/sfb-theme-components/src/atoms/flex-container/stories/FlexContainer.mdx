import { Meta, Story, Canvas } from '@storybook/blocks';
import * as FlexContainerStories from './FlexContainer.stories';

<Meta of={FlexContainerStories} />

# Flex Container

<br />

## `FlexContainer` automatically re-arranges the components passed based on screen size

### Default

<Canvas>
  <Story of={FlexContainerStories.Default} />
</Canvas>
