import React from 'react';
import Paging from '../Paging';
import { paginationMock, paginationShortMock } from '../../../../mocks/listingProducts/pagination';

export default {
    title: 'Atoms/Paging',
    component: Paging
};

export const Default = {
    render: () => <Paging data={paginationMock} />,
    name: 'Default'
};

export const $3Pages = {
    render: () => <Paging data={paginationShortMock} />,
    name: '3 pages'
};

export const Sizes = {
    render: () => <span>Small Size</span>,
    name: 'Sizes'
};
