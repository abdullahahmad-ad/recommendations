import React from 'react';
import SearchBar from '../SearchBar';
import { getArgTypes, getArgs } from '../../../tools/controlsHelper';

export default {
    title: 'DEPRECATED/Atoms/SearchBar (Deprecated)',
    component: SearchBar,
    argTypes: {
        ...getArgTypes(SearchBar.schema().form),
        borderColor: {
            control: 'color',
            description: 'The color of the border',
            if: { arg: 'borderVisible', eq: true }
        }
    },
    parameters: {
        controls: {
            include: Object.keys(getArgTypes(SearchBar.schema().form))
        }
    }
};

export const Default = {
    render: args => <SearchBar settings={args} />,
    args: getArgs(SearchBar.schema().form)
};
