import React from 'react';
import FlexContainer from '../FlexContainer';
import FlexContainerSchema from '../../../constants/atoms/schema/flex-container';
import CardStyle from '../../product-groups-tiles/card-style/CardStyle';
import { productGroupsTilesMocks } from '../../../../mocks/product/productGroupsTiles';
import { getArgTypes, getArgs } from '../../../tools/controlsHelper';

export default {
    title: 'Atoms (Preview)/Flex Container',
    component: FlexContainer,
    argTypes: getArgTypes(FlexContainerSchema.schema().form),
    parameters: {
        controls: {
            include: Object.keys(getArgTypes(FlexContainerSchema.schema().form))
        }
    }
};

export const Default = {
    render: args => (
        <FlexContainer
            component={CardStyle}
            itemList={productGroupsTilesMocks.data}
            flexContainerSettings={args}
        />
    ),
    args: getArgs(FlexContainerSchema.schema().form)
};
