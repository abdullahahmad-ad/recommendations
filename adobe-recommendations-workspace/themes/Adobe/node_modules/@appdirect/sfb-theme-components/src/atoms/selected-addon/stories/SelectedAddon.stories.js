import React from 'react';
import SelectedAddon from '../SelectedAddon';
import { selectedAddonsMock } from '../../../../mocks/product/configuratorSelectedAddons';

const addonData = selectedAddonsMock.configurator.selectedAddons[0];

export default {
    title: 'Atoms/Selected Addon',
    component: SelectedAddon
};

export const Default = {
    render: () => <SelectedAddon data={addonData} />,
    name: 'Default'
};
