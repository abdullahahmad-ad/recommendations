import { primaryNavMock } from './primary-nav';
import { secondaryNavMock } from './secondary-nav';
import { categoriesMultiLevelMock } from './primary-nav/categories';

export const navMock = {
    primary: primaryNavMock,
    secondary: secondaryNavMock
};

export const navWithCategoriesMock = {
    secondary: secondaryNavMock,
    primary: {
        ...primaryNavMock,
        categories: categoriesMultiLevelMock
    }
};
