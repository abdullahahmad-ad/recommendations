import { relatedContentMock } from '../product/related-content';
import { supportMock } from '../product/support';
import { customAttributesMock } from '../product/custom-attributes';
import { productOverviewMock as overviewMock } from '../product/overview';

export const defaultMock = {
    support: supportMock,
    relatedContent: relatedContentMock,
    customAttributes: customAttributesMock,
    overview: overviewMock
};

export const noSidebarMock = {
    support: supportMock,
    overview: overviewMock
};

export const noOverviewMock = {
    support: supportMock
};
